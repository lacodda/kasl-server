/target
/.env

# Web UI
frontend/node_modules/
# The built app. Nothing is kept here: `pnpm build` empties the directory, so a
# committed placeholder could not survive anyway - the embed tolerates its
# absence instead (ADR 0012).
frontend/dist/

# Docs site (Astro Starlight)
docs/node_modules/
docs/dist/
docs/.astro/

# Staging directory for `turnout deploy` (tools/stage-deploy.sh)
/deploy
