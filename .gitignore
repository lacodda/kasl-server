/target
/.env

# Web UI
frontend/node_modules/
# The built app, except the placeholder that keeps `cargo build` working in a
# checkout without Node - rust-embed needs the directory to exist (ADR 0012).
frontend/dist/*
!frontend/dist/.gitkeep

# Docs site (Astro Starlight)
docs/node_modules/
docs/dist/
docs/.astro/

# Staging directory for `turnout deploy` (tools/stage-deploy.sh)
/deploy
